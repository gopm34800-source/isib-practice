#include <bits/stdc++.h>
using namespace std;

int main() {
    int h,w;
    cin>>h>>w;
    vector<vector<int>> arr(h,vector<int>(w));
    for(int i=0;i<h;++i){
        for(int j=0;j<w;++j){
            cin>>arr[i][j];
        }
    }
    int min_row=55,min_col=55;
    int max_row=-1,max_col =-1;
}
